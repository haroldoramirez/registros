create table registro (
    id bigint generated by default as identity,
    data_cadastro timestamp(6),
    latitude float(53),
    longitude float(53),
    status varchar(255) check ((status in ('DIANA','LIAM','PASSEIO'))),
    titulo varchar(150) not null,
    primary key (id)
)